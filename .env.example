# AI Resume Ranking System - Environment Configuration Template
# AWS Serverless Architecture

# AWS Configuration
# Region where your AWS resources are deployed
AWS_REGION=us-east-1

# S3 Configuration
# S3 bucket name for resume storage (required for webhook lambda deployment)
# Replace with your actual bucket name from CDK deployment output
S3_BUCKET=your-resume-ranking-bucket-name

# Logging Configuration
# Log level for application logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Frontend Configuration (populated after CDK deployment)
# These values are automatically extracted from CDK outputs
USER_POOL_ID=us-east-1_YourUserPoolId
USER_POOL_CLIENT_ID=YourUserPoolClientId
IDENTITY_POOL_ID=us-east-1:your-identity-pool-id
GRAPHQL_ENDPOINT=https://your-appsync-id.appsync-api.us-east-1.amazonaws.com/graphql
API_ID=your-api-gateway-id

# Postmark Email Configuration
# Email notifications for completed resume analysis
POSTMARK_SERVER_TOKEN=your-postmark-server-token-here
NOTIFICATION_EMAIL=your-notification-email@example.com
FROM_EMAIL=your-from-email@example.com
